# Python dependencies for the Eligibility Dashboard Generator

# HTTP requests library for API calls and RPC
requests>=2.31.0

# Environment variable management from .env files
python-dotenv>=1.0.0

# Cryptography for keccak256 hashing (contract function selectors)
pycryptodome>=3.19.0

# Vercel Blob, for round-tripping run-to-run state on a serverless host.
# storage.py imports this lazily and only when BLOB_READ_WRITE_TOKEN is set, so
# it is never loaded by the Docker image or a local run.
vercel>=0.3.4
