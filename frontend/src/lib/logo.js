/**
 * The Graph's official logomark, imported from @graphprotocol/gds-icons so it
 * tracks the design system rather than being a copy that can drift.
 * `?raw` inlines the file contents at build time.
 */
export { default as theGraphLogo } from '@graphprotocol/gds-icons/assets/custom/the-graph-logo.svg?raw'
